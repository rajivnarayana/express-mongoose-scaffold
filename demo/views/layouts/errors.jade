if html.errors && html.errors.length > 0
    .alert.alert-danger
        a(href="#" class="close" data-dismiss="alert") &times;
        each error in html.errors
            .error #{error.error || error.message}